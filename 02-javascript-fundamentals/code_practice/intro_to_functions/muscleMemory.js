/**

Instructions:

Practice typing the syntax for functions.
Write 10 functions with different messages and console log them. Focus on using proper style and indentation.
Be sure to test your code by running it after each repetition. You'll be surprised and also annoyed at what details you missed!
Learn from those typos now and make your life easier.

Initial code:

function sayHello(name) {
  let msg = "Hello, " + name + ". How are you?";
  return msg;
}

*/

// One
function sayHello(name) {
  let msg = `Hello, ${name}. How are you?`;
  return msg;
}

// Two
function sayGoodbye(name) {
  let msg = `Bye, ${name}! See you later.`;
  return msg;
}

// Three
function myFavoriteGame(gameName) {
  let msg = `My favorite game is ${gameName}.`;
  return msg;
}

// Four
function myFavoriteIceCream(iceCream) {
  let msg = `My favorite ice cream is ${iceCream}.`;
  return msg;
}

// Five
function petsName(petName) {
  let msg = `My pet's name is ${petName}.`;
  return msg;
}

// Six
function myFavoriteBook(book) {
  let msg = `My favorite book is ${book}.`;
  return msg;
}

// Seven
function sayGoodJob(name) {
  let msg = `Hey, ${name}! Good job working through App Academy.`;
  return msg;
}

// Eight
function myFavoriteSport(sport) {
  let msg = `My favorite sport is ${sport}.`;
  return msg;
}

// Nine
function myFavoriteHobby(hobby) {
  let msg = `I like doing ${hobby} as a hobby.`;
  return msg;
}

// Ten
function pcOrMac(computer) {
  let msg = `My use a ${computer} computer.`;
  return msg;
}

console.log(sayHello("John"));
console.log(sayGoodbye("Sam"));
console.log(myFavoriteGame("Rummy"));
console.log(myFavoriteIceCream("Chocolate"));
console.log(petsName("Auggie"));
console.log(myFavoriteBook("The Bible"));
console.log(sayGoodJob("Julie"));
console.log(myFavoriteSport("Kickboxing"));
console.log(myFavoriteHobby("Hiking"));
console.log(pcOrMac('Windows'));
